{% include 'breadcrumbs' %}

<div class="container">
	<div class="row">
		<div class="col-md-12">
			<div class="product-header-bottom-line">
				<div class="row">
					<div class="col-md-8">
						<h1 class="tb-h-underline product-h1 mb-0">{{ product.title }}</h1>
					</div>
				</div>
			</div>
		</div>
	</div>
	<div class="row">
		<div class="col-md-12">
			{% assign product_vendor_handle = product.vendor | handle %}       
			{% if collections[product_vendor_handle].handle == product_vendor_handle %}
				{% assign vendor_url = collections[product_vendor_handle].url %}
			{% else %}
				{% assign vendor_url = product.vendor | url_for_vendor %}
			{% endif %}
			<h6 class="mt-2 pd-pg-vendor"><a href="{{ vendor_url }}">{{ product.vendor }}</a></h6>
		</div>
	</div>
	<div class="row">
		<div class="col-lg-8 col-md-7">
			<div class="row">
				{% include 'product-left-column' %}
			</div>
		</div>
		<div class="col-lg-4 col-md-5">
			<div class="row">
				{% include 'product-right-column' %}
			</div>
		</div>
	</div>
	{% include 'product-description' %}
	{% include 'product-related' %}
	{% include 'product-reviews' %}
</div>